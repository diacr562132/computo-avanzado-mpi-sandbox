{
  "name": "Computo Avanzado MPI Sandbox",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "postCreateCommand": "python3 --version && mpirun --version && python3 -c \"from mpi4py import MPI; print('mpi4py funcionando')\"",
  "remoteUser": "root"
}
